package tn.esprit.controller.course;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.control.Alert;
import javafx.scene.control.TextField;
import javafx.stage.Stage;
import tn.esprit.models.Course;
import tn.esprit.services.CourseService;

import java.io.IOException;

    public class DeleteCourse {
           /* CourseService courseService = new CourseService();

            // Création d'un objet Course avec l'ID du cours à supprimer
            Course courseToDelete = new Course();
            courseToDelete.setId(1); // ID du cours à supprimer

            // Appel de la méthode delete de CourseService pour supprimer le cours
            courseService.delete(courseToDelete);
        }
    }*/

}
