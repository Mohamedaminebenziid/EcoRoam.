{% extends 'dashboard.html.twig' %}

{% block title %}forum{% endblock %}

{% block body %}
    <h1>forum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>title</th>
                <td>{{ forum.title }}</td>
            </tr>
            
            <tr>
                <th>description</th>
                <td>{{ forum.description }}</td>
            </tr>
           
        </tbody>
    </table>

    <a href="{{ path('app_forumfront_index') }}">back to list</a>

{% endblock %}
